<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uob.gwb.pbp.dao.aes.AesQueryDao">

    <select id="getCompanyIdFromName" resultType="java.lang.Long" statementType="PREPARED">
        <![CDATA[
            SELECT company_id FROM aes_company_master WHERE company_name = #{companyName}
        ]]>
    </select>

    <select id="getGebCompanyId" resultType="java.lang.Long" statementType="PREPARED">
        <![CDATA[
            SELECT id_value FROM aes_company_alternate_identifiers WHERE id_type = 'GEB' AND company_id = #{companyId}
        ]]>
    </select>

    <select id="getRejectFileOnError" resultType="java.lang.String" statementType="PREPARED">
        <![CDATA[
            SELECT reject_file_on_error FROM geb_company_attribute_mvr WHERE company_id = #{gebCompanyId}
        ]]>
    </select>

    <select id="getBatchBookingIndicator" resultType="java.lang.String" statementType="PREPARED">
        <![CDATA[
            SELECT batch_booking_indicator FROM aes_company_setups WHERE company_id = #{companyId} AND resource_id = #{resourceId}
        ]]>
    </select>

</mapper>