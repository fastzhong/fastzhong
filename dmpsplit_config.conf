
#!/bin/bash
# File: config.conf

# Directory configurations
SRC_DIR="/CEWTH/QR/BULK/Outgoing/processing"
NORMAL_DIR="/sftp/normal"
LARGE_DIR="/sftp/large"
BACKUP_DIR="/CEWTH/QR/BULK/Outgoing/backup"
ERROR_DIR="/CEWTH/QR/BULK/Outgoing/error"
LOG_DIR="/var/log/filewatcher"

# Log file name pattern (can include date format variables)
# For example: filewatcher_%Y%m%d.log will create logs like filewatcher_20250408.log
LOG_FILE_PATTERN="filewatcher_%Y%m%d.log"

# File size threshold in bytes (5MB = 5242880 bytes)
SIZE_THRESHOLD=5242880

# Time between watch cycles in seconds (if using polling mode)
POLL_INTERVAL=5

